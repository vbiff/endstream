/// Sound effect identifiers for one-shot audio.
enum SfxType {
  buttonTap,
  cardPlay,
  cardDraw,
  attackHit,
  operatorEliminated,
  turnStart,
  turnEnd,
  abilityActivate,
  victory,
  defeat,
  matchFound,
  error,
}

/// Background music track identifiers.
enum MusicTrack {
  menu,
  battle,
  ambient,
}
